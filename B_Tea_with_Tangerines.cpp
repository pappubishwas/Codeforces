#include <bits/stdc++.h>
#include<iostream>
#include<algorithm>
#include <numeric>
using namespace std;
#define ll long long
#define ld long double
#define endl '\n'
void solve()
{
ll n,p=0,i,q;
cin>>n;
ll a[n];
for(i=0;i<n;i++)cin>>a[i];

for(i=1;i<n;i++){
    q=a[i]/2;
    if(q>a[0] && q<)
}
p+=(a[i]/a[0]-1);
cout<<p<<endl;
}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    ll t;
    cin >> t;
    while (t--)
    {
        solve();
    }
    return 0;
}