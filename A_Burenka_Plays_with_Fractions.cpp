#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define ld long double
#define endl '\n'
void solve()
{
// long long int a,b,c,d;
// cin>>a>>b>>c>>d;
// long long int p,t,s;
// p=(a*d)%(c*b);
// t=((c*b)%(a*d));
// s=c*b;
// if((a==0 && c==0) || (a*d)==(b*c))
// cout<<0<<endl;
// else if((a==0) || (c==0) || ((a%c==0) && (b%d==0 || d%b==0)) || ((c%a==0 || a%c==0) && (b%d==0)))
// cout<<1<<endl;
// else
// cout<<2<<endl;
    

}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    ll t;
    cin >> t;
    while (t--)
    {
        solve();
    }
    return 0;
}